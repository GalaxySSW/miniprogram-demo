# 小程序开发规则

## 运行与验证

- 这是原生微信小程序，入口配置在 `app.json`，页面由 WXML/WXSS/JS/JSON 组成。
- 默认使用本地 mock 流程验证：`pages/home/home` → 立案 → `pages/evidence/evidence` → 后续演示链路。
- 需要操作微信开发者工具时，优先使用 `weapp-agent-mcp` 的结构化工具：
  `mp_ensureConnection` → `mp_healthCheck` → 页面读取/元素操作 → 路由或数据断言。
- 对已有页面优先按文本和稳定 class 定位；新加交互时尽量补充稳定的 `qa-*` 选择器，避免依赖坐标。
- 页面操作后至少校验一次真实路由或关键数据，不把点击成功等同于业务成功。
- 复杂流程拆成短步骤；截图调用保持串行。

## 修改边界

- 除非用户明确要求，不改变 mock 演示的默认入口，不接入真实账号、支付或生产云环境。
- 修改 JavaScript/WXML 后，先做静态检查，再用微信开发者工具编译；如果 MCP 可用，再执行最小冒烟路径。
- 不执行上传、提审、发布、删除云数据或发送真实外部 AI 请求。

## Git 协作

- `rainno` 是本项目日常迭代、Codex 开发和功能合并的唯一工作分支。
- 默认直接在 `rainno` 上开发，不为单个功能创建新的 `feat/*` 或 `fix/*` 分支。
- 修改前先检查 `git status`，保留用户已有的未提交改动，不擅自覆盖或清理。
- `main` 仅作为稳定基线；向上游仓库提交 Pull Request 前，先在 `rainno` 完成本地验证和人工确认。
- 除非用户明确要求，不自动提交、推送、创建 Pull Request 或合并分支。
